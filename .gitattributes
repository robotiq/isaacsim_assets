*.usd filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text

# Config layers kept as ASCII text under their original .usd name (diffable, not LFS).
# These overrides must stay AFTER the blanket *.usd rule so they win.
grippers/Gripper_2F85/Robotiq_2F_85_edit.usd  !filter !diff !merge text
grippers/Gripper_2F85/materials/materials.usd !filter !diff !merge text
